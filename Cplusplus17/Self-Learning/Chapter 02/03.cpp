#include <iostream>
// 提前声明
using std::cin;
using std::cout;
using std::endl;

int main()
{
    int a{};
    // cin >> a;
    cout << a << endl;
    // scanf()、printf()
    /*
        %d      int a
        %ld     long b
        %c      char c
        %f      float d
        %lf     double e
    */
    /*
        scanf("%d %ld %c %f %lf", &a, &b, &c, &d, &e);
        printf("%d %ld %c %f %lf", a, b, c, d, e);
    */

    return 0;
}
